# NX SDDM theme

The NX login screen — Plasma's Breeze SDDM theme forked with its QML kept verbatim, repointed at the NX field background and shipping `NX.colors`.

SDDM themes live outside `$HOME`, so installing needs root.

```sh
sudo cp -r sddm/nx /usr/share/sddm/themes/nx
```
